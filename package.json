{
  "name": "xamarin-v8",
  "version": "1.0.0",
  "description": "V8 Bindings for Xamarin for Android (iOS coming soon, only for local development)",
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean": "rm -rf dist; rm -rf build",
    "export_npm_env": "./scripts/export_npm_env.sh",
    "build": "bash -c '. ./scripts/export_npm_env.sh && ./scripts/build.sh'"
  },
  "config": {
    "RN_VERSION": "master"
  },
  "dependencies": {
    "v8-android": "~8.80.1"
  },
  "peerDependencies": {
    "react-native": "*"
  },
  "devDependencies": {
    "clang-format": "^1.4.0"
  }
}
